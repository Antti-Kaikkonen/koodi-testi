# koodi-testi

Run the backend before running the frontend. The instructions use Yarn package manager but using npm should work too. For example `yarn install` -> `npm install`,  `yarn start` -> `npm run start` and `yarn build` -> `npm run build`  

## Running the node.js backend 
Follow [these](koodi-testi-backend/README.md) instructions

## Running the React frontend
Follow [these](koodi-testi-frontend/README.md) instructions
